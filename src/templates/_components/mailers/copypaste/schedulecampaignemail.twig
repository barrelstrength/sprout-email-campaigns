{% import "_includes/forms" as forms %}

<div class="body">

    <h1>{{ "Copy & Paste"|t('sprout-campaigns') }}</h1>

    {% set dateScheduled = craft.sproutEmail.getDisplayDateScheduled(email) %}

    {% if dateScheduled is not empty %}
        <div style="float:left; margin-bottom: 20px;">
            <strong>{{ "Date Scheduled"|t('sprout-campaigns') }}
                :</strong> {{ dateScheduled|date("F jS Y \\a\\t g:ia") }}
        </div>
        <div style="clear: both"></div>
    {% endif %}

    <div style="position:relative;">
        <div style="float:left;">
            <strong>{{ "HTML Content"|t('sprout-campaigns') }}</strong></div>

        <div style="float:right;margin:-2px 0 5px;">
            <a href="#" class="btnSelectAll btn small formsubmit"
                data-clipboard-target-id="html-content"
                data-success-message="{{ 'HTML content copied to your clipboard.'|t('sprout-campaigns') }}"
                title="{{ 'Select HTML Content'|t('sprout-campaigns') }}">
                {{ "Select HTML"|t('sprout-campaigns') }}
            </a>
        </div>
    </div>

    <div>
        {{ forms.textarea({
            id: "html-content",
            name: "html",
            value: html,
            rows: 5,
            class: "code textfield",
            autofocus: true,
        }) }}
    </div>

    <div style="position:relative;margin-top:30px;">
        <div style="float:left;">
            <strong>{{ "Text Content"|t('sprout-campaigns') }}</strong></div>

        <div style="float:right;margin:-2px 0 5px;">
            <a href="#" class="btnSelectAll btn small formsubmit"
                data-clipboard-target-id="text-content"
                data-success-message="{{ 'Text content copied to your clipboard.'|t('sprout-campaigns') }}"
                title="{{ 'Select Text Content'|t('sprout-campaigns') }}">
                {{ "Select Text"|t('sprout-campaigns') }}
            </a>
        </div>
    </div>

    <div>
        {{ forms.textarea({
            id: "text-content",
            name: "text",
            value: text,
            rows: 5,
            class: "code textfield",
        }) }}
    </div>

</div>

<div class="footer">

    <div class="buttons left">
        <button id="close" class="btn">{{ "Cancel"|t('sprout-campaigns') }}</button>
    </div>
    <div class="buttons right actions">
        <button
            data-action="sprout-campaign/copy-paste/mark-sent"
            data-email-id="{{ email.id }}"
            class="btn submit">
            {{ "Mark as Sent"|t('sprout-campaigns') }}
        </button>
    </div>

</div>