{% extends "sprout-base-email/_layouts/elementindex" %}
{% set title = "Campaign Email"|t('sprout-campaigns') %}
{% set elementType = 'barrelstrength\\sproutcampaigns\\elements\\CampaignEmail' %}

{% block actionButton %}
    {% set campaignTypes = craft.sproutEmail.getCampaignTypes() %}

    <div class="buttons">
        <div class="btngroup submit">

            {% if campaignTypes|length %}

                <div class="btn submit menubtn add icon">{{ "New Campaign"|t('sprout-campaigns') }}</div>
                <div class="menu">
                    <ul>
                        {% for campaignType in campaignTypes %}
                            <li>
                                <a href="{{ url('sprout-campaign/' ~ campaignType.id ~ '/new') }}">{{ campaignType.name|t('sprout-campaigns') }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </div>

            {% else %}

                <a class="btn submit add icon"
                    href="{{ url('sprout-campaign/settings/campaigntypes/edit/new') }}">{{ "New Campaign Type"|t('sprout-campaigns') }}</a>

            {% endif %}
        </div>
    </div>
{% endblock %}

{% block content %}

    {{ parent() }}

    {% include "sprout-base-email/_modals/base" %}

{% endblock %}